<section class="card card--status">
  <h1 class="card__title">{{page.heading}}</h1>
  <p class="card__body">{{page.body}}</p>
  <p class="card__actions">
    <a class="btn btn--primary" href="{{urls.trustedDevicesManage}}">{{common.back}}</a>
  </p>
</section>
